<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ThesisApplication.Pages.SearchPage"
             Title="Kereső"
             >

    <!-- SEARCH PAGE-->

    <StackLayout
        Margin="12"
        >
        <StackLayout Orientation="Horizontal">
            <Entry
                x:Name="SearchBar"
                Placeholder="Keresés..."
                HorizontalOptions="FillAndExpand"
            />
            <Button WidthRequest="48"
                x:Name="filters"
                Command="{Binding ClickFilter}"
            />
        </StackLayout>

        <ScrollView>
            <StackLayout
                x:Name="ResultList"
                >
            </StackLayout>
        </ScrollView>
    </StackLayout>

</ContentPage>